import styled from 'styled-components'

const SharedSpace = () => {
    return (
        <StyledSharedSpace>

        </StyledSharedSpace>
    )
}

const StyledSharedSpace = styled(SharedSpace)`
  border: 1px solid #000;  
  width: 700px;
  height: 500px;
`

export default StyledSharedSpace
